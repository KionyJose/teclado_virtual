name: teclado_virtual
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1
  
dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.8
  # Minhas Modificaçoes =============== INI
  ffi: ^2.0.0
  window_manager: ^0.4.3
  provider: ^6.1.2
  xinput_gamepad: ^1.3.1
  screen_retriever: ^0.2.0
  # Minhas Modificaçoes =============== FIM

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true

